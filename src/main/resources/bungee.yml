name: ${project.artifactId}
main: com.mcmiddleearth.connect.bungee.ConnectBungeePlugin
version: ${project.version}
author: Eriol_Eandur

permissions:
    mcmeconnect.tp:
        description: allows a player to use /tp across servers
    mcmeconnect.world:
        description: addition of .<servername> allows a player to /tp or /mvtp cross servers
    mcmeconnect.watchdog:
        description: players with this permission will recive warings about server downs